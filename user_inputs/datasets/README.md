# Dataset Input Folder

Put CSV files for local testing in this folder.

Then run:

```cmd
test_data_source.bat
```

Choose option `1. Test with local CSV dataset`, enter a table name, and enter the CSV file name.
